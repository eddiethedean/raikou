# Roadmap

- Expand expression coverage (aggregates, case/when, more string/date functions).
- Improve schema descriptor fidelity from Python type annotations.
- Add Spark sinks and partitioning options.
- Add a richer `raikou` high-level API (I/O helpers, streaming collection, etc.).

